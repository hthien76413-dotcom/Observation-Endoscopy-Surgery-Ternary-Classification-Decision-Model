# 项目记忆

## 研究范围

本仓库只做一件事：**儿童消化道异物「观察 / 内镜 / 手术」三分类决策模型**，
及其 SCI 论文产出。数据源为 `10年消化道异物原始数据.xlsx`（2016-07—2026-06，
1238 例住院 / 1228 例患儿）。

## 重要：磁性异物论文已单独完成，不再规划

用户（儿外科医师）已就**磁性异物（磁力珠 / 巴克球）流行病学**单独写好论文，
该选题**不再作为本项目的产出**。

- 不要再建议「拆第二篇写磁性异物」「配套快刊」之类的方案
- 不要为磁性异物单独规划期刊、图表或时间表

磁性异物的**数据发现仍然有效**，且是三分类模型的核心预测因子，继续保留在模型中：
磁性异物占比 2016 年 0% → 2020 年 22.0%；穿孔率 29.3%（44/150）vs 其他异物
3.1%（34/1094）；占全队列 78 例穿孔的 56%；手术率 40.7%。

**但在主论文中的处理方式变了**：这些内容不再作为本文的新发现呈现，而应
引用用户已完成的磁性异物论文。由于两篇文章使用同一队列，投稿时须按重叠发表
规范处理——在 Methods 中声明队列已部分报道并引用该文，说明本文研究问题不同
（治疗决策预测 vs 流行病学描述），投稿信中主动披露，必要时随附该文。

### 该文状态：已写好，拟投 JPGN，尚未投出（截至 2026-09）

**期刊分流（已给出建议）**：磁性异物 → **JPGN**（按用户原计划，尽快投出）；
本文 → **J Pediatr Surg** 首选，其后 Surgical Endoscopy → World J Emerg Surg →
Pediatr Surg Int → Front Pediatr。**本文不去 JPGN**，以避开同编辑同队列撞车；
仅在磁性异物文已见刊且前几家均未中时，才把 JPGN 作为后备。

时序要点（详见 `docs/SCI论文写作方案.md` §四、§五）：

- **磁性异物论文越早投越好**。此刻本文尚未送审，其投稿系统「是否有相关稿件他处在审」
  可如实答「否」，是零披露负担的干净投稿；本文一旦送审，该窗口即关闭。
- **本文不等它**。本文工期由 X 线重标注主导，与该文审稿周期互不阻塞。
- 已建议把该文挂 medRxiv 预印本，以获得可引用的 DOI。
- 两篇不可同期投同一家期刊。
- **数字口径必须一致**。本仓库的数字来自脚本正则抽取，该文可能是人工计数，
  两者可能已经不一致。以该文的最终数字为准回改脚本，不要反过来。
  该文每次修回后都要重新核对——若其纳入标准变动（如剔除再入院、
  改用患儿而非住院次数为单位），分母会变，本文须同步跟改。
- Fig 2（十年趋势）与 Table 4 有与该文重复图表的风险，定稿前逐一核对。

## 数据陷阱（改动代码前务必知道）

1. **内镜取异物记录在手术记录与手麻系统，不在 `内镜报告` 表**。该表仅 18 条，
   实际内镜 520 例——本中心儿童内镜取异物在全麻下于手术室完成。按内镜报告表
   判定标签会把 520 例内镜整体误判为观察组。
2. **CRP 为左删失数据**。低于检测限的结果在定性列记为 `<0.78`、定量列为空。
   仅读定量列会使缺失率从 51.6% 虚高到 80.0%。已按 LOD/2 代入。
3. **X 线定位文本质量差**。1073 例有报告，557 例无法定位到具体消化道节段，
   剔除决策后片后，影像变量块对模型毫无贡献（加入后宏平均 AUC 反降 0.003）。重标注表单已生成，见
   `annotation/X线重标注表单.xlsx` 与 `docs/X线重标注手册.md`。
4. **`住院病历手术记录.手术日期及时间` 只有日期，100% 为 00:00**。任何时序判定
   （如「X 线是否拍于手术之前」）必须用 `手麻系统信息.手术开始时间`（精确到分钟）；
   用前者会把几乎所有片子误判为「决策后」。无手麻记录时只能降级为同日判定。
5. **首次 X 线未必在决策之前**。`build_cohort.py` 已做时相过滤（`decision_phase()`）：
   首次片按**检查时间**（非报告时间）排序，晚于手术开始者影像特征一律置空。
   队列内可用影像 981 例（术前 484 + 无手术 497），决策后 49、同日不明 37、无影像 171。
   同一过滤也应用于化验（按报告时间，偏保守），决策前化验可用 1116 例。
   分支行为由 `scripts/test_decision_phase.py` 锁定，改动前先跑。

## 缺失机制（重要，影响统计方案）

**缺失本身携带治疗信息。** 内镜组 85% 没有 CRP（快速送手术室取异物，来不及做化验），
观察组 55.2%。仅用缺失指示变量就能把治疗方式预测到宏平均 AUC 0.602。

因此：① 必须多重插补（MICE，插补模型纳入结局），不可单一值填补；
② 缺失指示变量绝不可作预测因子；③ 完整病例分析无效（n=299，构成偏移 24%），
只能作负面对照。**CRP 已按用户决定剔除出预测因子**（`pilot_model.py` 的 `LAB_FEATURES`），
但仍保留在 `cohort.csv` 中，供 Table 1 描述、缺失诊断与敏感性分析 S6 使用——不要整个删掉。
剔除后完整病例分析从 299 例（偏移 24%）增至 862 例（偏移 4.2%），由负面对照变为有效分析。

诊断由 `scripts/pilot_model.py` 的 `missingness_diagnostics()` 自动输出。

## 自由文本抽取纪律

**异物类型词典按实测归纳，勿凭空简化。** 初版未归类 282 例(22.7%)，补词后降至
184 例(14.9%)——缺的是词不是信息。两处易错：「骨」必须写成「鱼骨|鸡骨|猪骨|骨头」
（裸「骨」会命中「胸骨后疼痛」「喉软骨软化」）；「针」逐词列举
（「针灸针」是异物，「做针灸时」不是）。核验表单见
`annotation/异物类型核验表单.xlsx`，回流用 `scripts/ingest_fbtype.py`。

**必须处理否定语境。** 病历模板总把体征写全，「全腹压痛阴性，反跳痛阴性」里
同样含「压痛」「反跳痛」。早期版本据此得出压痛阳性率 99.5%(1232/1238)，
实际仅 11.0%(136/1238)。现用分句级否定作用域（`assert_positive()`）+
阴阳记法判读（`sign_positive()`）。

**体征只取 `专科情况（体检）`**（腹部专科查体）。`体格检查` 是全身查体，
含乳突、副鼻窦、甲状腺的压痛描述，混入会把耳鼻喉体征当成腹部体征。

行为由 `scripts/test_symptom_extraction.py` 的 21 个用例锁定，改动前先跑。

## 建模纪律

**预测因子必须限定在治疗决策时点之前。** 术中诊断、出院诊断、手术记录内容、
住院天数、并发症均为决策后信息，严禁进入模型，只用于结局定义与标签校验。
化验取入院后首次结果。

## 论文核心论点

手术 AUC 0.91–0.95，而观察／内镜边界仅 0.72–0.74。这不是模型缺陷，而是临床真相：
是否手术由客观病理状态决定，胃内钝性异物取不取则含实践差异成分。
此点写进 Discussion 核心位置，不要当作局限性掩盖。

## 用户

儿外科医师。中文沟通。
